name: Keep HF Space Awake

on:
  schedule:

    - cron: '0 */6 * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Space
        run: curl -sf https://YOUR_USERNAME-expense-agent.hf.space/ui/ || exit 1
